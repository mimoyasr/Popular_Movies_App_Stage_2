<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <ListPreference
        android:key="@string/pref_sort_by"
        android:title="@string/pref_sort_by_title"
        android:entries="@array/pref_sort_by_names"
        android:entryValues="@array/pref_sort_by_valuse"
        android:summary="Choose if Home page will sory by Most Popular or Top rated movies"/>

</PreferenceScreen>
